# Terraform state files and local cache
*.tfstate
*.tfstate.backup
*.tfstate.lock.info
.terraform/
.terraform.lock.hcl

# Terraform variable files (may contain secrets)
*.tfvars
*.tfvars.json

# Crash logs
crash.log
crash.*.log

# OS artifacts
.DS_Store
Thumbs.db

# Local deployment guide may contain environment-specific values
docs/deployment-guide.md
# Session troubleshooting log — local only, not for public repo
docs/session-troubleshooting-log.md
# EKS and ArgoCD deployment notes — local only
docs/eks-argocd-deployment-notes.md
# Local Docker Jenkins secrets and runtime files
docker/jenkins-local/.env
argocd-next-steps.md